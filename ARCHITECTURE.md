# Cross-system stewardship

For a change to an API client, endpoint, generated DTO, data pipeline, or
authentication boundary, consult `../architecture-docs` before implementation.
Update its evidence catalog in the same change and run `npm run validate`
there. Code, schemas, migrations, and tests remain the behavioral truth.

