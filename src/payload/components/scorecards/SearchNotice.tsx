export function SearchNotice() {
  return (
    <p
      style={{
        margin: 0,
        padding: 16,
        borderRadius: 6,
        background: 'var(--theme-elevation-50)',
      }}
    >
      Newly added or updated taxonomies may take up to one hour to appear in
      search results due to cache refresh timing.
    </p>
  );
}
